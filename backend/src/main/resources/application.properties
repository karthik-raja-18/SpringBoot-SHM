# =======================
# Application Information
# =======================
spring.application.name=secondhand-marketplace

# =======================
# Datasource Configuration
# =======================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:35290/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=bZWbVhWDcNFhrfVFiMWQtguPzNPDPzsi

# =======================
# JPA Configuration
# =======================
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# =======================
# JWT Configuration
# =======================
jwt.secret=ThisIsMyVerySecureAndLongJwtSecretUsedOnlyForSigningTokensInSpringBootAndForItsSecurity!
jwt.expiration=86400000

# =======================
# CORS Configuration
# =======================
cors.allowed-origins=https://smartbuy-puce.vercel.app,http://localhost:3000

# =======================
# Logging Configuration
# =======================
logging.level.com.example.SecondHandMarketplace=DEBUG
logging.level.org.springframework.security=DEBUG